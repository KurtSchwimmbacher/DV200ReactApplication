runtime: nodejs20 # or the latest version you want to use
handlers:
  - url: /.*
    static_files: build/index.html
    upload: build/index.html
  - url: /(.*)
    static_files: build/\1
    upload: build/(.*)
